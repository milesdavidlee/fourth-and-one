// Variable font loading via Fontsource. Inter Tight covers display weights;
// Inter covers body. System fonts (SF Pro) are still preferred per --font-* tokens.
import '@fontsource-variable/inter';
import '@fontsource-variable/inter-tight';
